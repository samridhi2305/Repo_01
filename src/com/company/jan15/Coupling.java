package com.company.jan15;
//Coupling refers to the dependency of one class in another class.
//If one class is dependent on another and modification of one class affects another class then it is called Strong coupling.
//but if we can modify one class(Address) without affecting other(Student) then both classes are loosely coupled.
//Loose coupling is achieved using Spring framework.
public class Coupling {
}
